# Quickstart

```bash
./apm hello.apex
./apm run examples/hello.apex
./apm check examples/fib.apex
./apm fmt examples/hello.apex
./apm repl
```

## Syntax

```apex
let name = "Apex"

fn greet(who):
    print("Hola " + who)

if name == "Apex":
    greet(name)
```
